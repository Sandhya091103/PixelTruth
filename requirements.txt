# PixelTruth dependencies.
#
# Configured for DEPLOYMENT on CPU-only hosts (Streamlit Community Cloud,
# Hugging Face Spaces). PyTorch is pulled from the official CPU wheel index, so
# torch/torchvision resolve to CPU builds; everything else comes from PyPI.
#
# For LOCAL GPU training instead, install the CUDA build:
#   pip install torch==2.6.0+cu124 torchvision==0.21.0+cu124 \
#       --extra-index-url https://download.pytorch.org/whl/cu124

--index-url https://download.pytorch.org/whl/cpu
--extra-index-url https://pypi.org/simple

# --- Deep learning core (CPU build) ---
torch
torchvision

# --- Data & numerics ---
numpy
pillow

# --- Metrics & evaluation ---
scikit-learn

# --- Plots ---
matplotlib
seaborn

# --- Training UX ---
tqdm

# --- Interpretability ---
grad-cam

# --- Demo ---
streamlit
